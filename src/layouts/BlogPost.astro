---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Giscus from '../components/Giscus.astro';
import Header from '../components/Header.astro';
import { useTranslations } from '../i18n/ui';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  updatedDate?: Date;
  category?: string;
  difficulty?: string;
  timeToRead?: number;
  locale: string;
}

const {
  title,
  description,
  pubDate,
  updatedDate,
  category,
  difficulty,
  timeToRead,
  locale,
} = Astro.props;
const t = useTranslations(locale);
---

<!doctype html>
<html lang={t('site.lang')} dir="ltr">
  <head>
    <BaseHead title={title} description={description} locale={locale} />
  </head>
  <body>
    <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[200] focus:rounded focus:bg-[var(--color-paper)] focus:px-4 focus:py-2 focus:text-sm focus:font-medium focus:text-[var(--color-ink)] focus:shadow-lg focus:outline-none">
      {locale === 'zh-CN' ? '跳到主要内容' : 'Skip to content'}
    </a>
    <Header locale={locale} />
    <main id="main-content">
      <article>
        <!-- Article header: full-bleed with distinct background -->
        <div class="bg-[var(--color-paper-light)]">
          <div class="section-inner">
            <header class="mb-10">
              <div class="post-meta mb-3">
                {t('blog.postedOn')} <FormattedDate date={pubDate} locale={locale} />
                {updatedDate && (
                  <span class="ml-2">· {t('blog.updatedOn')} <FormattedDate date={updatedDate} locale={locale} /></span>
                )}
                {category && <span class="ml-2">· {category}</span>}
                {difficulty && <span class="ml-2 text-[var(--color-ink-faint)]">[{difficulty}]</span>}
                {timeToRead && <span class="ml-2">· {timeToRead} {t('blog.minRead')}</span>}
              </div>
              <h1 class="font-serif text-3xl leading-tight md:text-4xl">{title}</h1>
              <p class="mt-4 max-w-xl text-sm leading-relaxed text-[var(--color-ink-muted)]">
                {description}
              </p>
            </header>
          </div>
        </div>

        <!-- Article body: constrained prose -->
        <div class="section-inner">
          <hr class="mt-0 mb-10" />

          <div class="prose">
            <slot />
          </div>

          <Giscus locale={locale} />
        </div>
      </article>
    </main>
    <Footer locale={locale} />
  </body>
</html>
